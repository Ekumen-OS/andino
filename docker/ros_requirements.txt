# ROS dependencies for andino packages
# These are pre-installed to speed up rosdep install

# andino_base dependencies
ros-${ROS_DISTRO}-hardware-interface
ros-${ROS_DISTRO}-rclcpp
ros-${ROS_DISTRO}-rclcpp-lifecycle
ros-${ROS_DISTRO}-pluginlib
ros-${ROS_DISTRO}-controller-manager
ros-${ROS_DISTRO}-joint-state-broadcaster

# andino_bringup dependencies
ros-${ROS_DISTRO}-rplidar-ros
ros-${ROS_DISTRO}-teleop-twist-joy
ros-${ROS_DISTRO}-teleop-twist-keyboard
ros-${ROS_DISTRO}-joy-linux
ros-${ROS_DISTRO}-v4l2-camera
ros-${ROS_DISTRO}-laser-filters
ros-${ROS_DISTRO}-rosbag2-storage-mcap

# andino_control dependencies
ros-${ROS_DISTRO}-diff-drive-controller
ros-${ROS_DISTRO}-ros2controlcli
ros-${ROS_DISTRO}-topic-tools

# andino_description dependencies
ros-${ROS_DISTRO}-joint-state-publisher-gui
ros-${ROS_DISTRO}-robot-state-publisher
ros-${ROS_DISTRO}-ros2launch
ros-${ROS_DISTRO}-rviz2
ros-${ROS_DISTRO}-xacro

# andino_navigation dependencies
ros-${ROS_DISTRO}-launch-ros
ros-${ROS_DISTRO}-navigation2
ros-${ROS_DISTRO}-nav2-bringup

# andino_slam dependencies
ros-${ROS_DISTRO}-slam-toolbox

# Test dependencies
ros-${ROS_DISTRO}-ament-cmake-clang-format
ros-${ROS_DISTRO}-ament-lint-auto
ros-${ROS_DISTRO}-ament-lint-common
ros-${ROS_DISTRO}-ament-cmake-pytest
ros-${ROS_DISTRO}-ament-index-python
